# WatchOver Frontend

The main project documentation, setup instructions, architecture, and API reference are available in the repository-level [README](../README.md).

The frontend is a React dashboard that polls the WatchOver Flask API every five seconds and displays CPU, memory, disk, and system-health information.

## Commands

```powershell
npm install
npm start
npm test -- --watchAll=false
npm run build
```
